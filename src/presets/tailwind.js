/**
 * Tailwind-compatible utility class map.
 * Does not depend on Tailwind — use these utilities when Tailwind is already on the page.
 */
export const tailwindPreset = {
  fieldContainer: null,
  errorClass: 'border-red-500 focus:border-red-500 focus:ring-red-500',
  errorMessageClass: 'mt-1 text-sm text-red-600',
  errorElement: 'p',
};
