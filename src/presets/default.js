/**
 * Neutral preset — pair with styles/default.css (optional).
 */
export const defaultPreset = {
  fieldContainer: '.vj-field',
  errorClass: 'vj-invalid',
  errorMessageClass: 'vj-error',
  errorElement: 'span',
};
